import styled from "styled-components";

const DetailTop = () => {
    return (
        <TopContainer>
            
        </TopContainer>
    )
}

export default DetailTop;

const TopContainer = styled.div`
    display: flex;
`